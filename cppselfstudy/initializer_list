#include <iostream>
#include <list>
#include <initializer_list>
using namespace std;
class Ctest{
public:
    Ctest(std::initializer_list<long double> il){ m_list = il; }
    int getNum(){ return m_list.size();}

private:
    list<long double> m_list;
};

